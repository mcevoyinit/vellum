/**
 * @vellum/ui/auth — Authentication provider
 *
 * Planned exports:
 *   - VellumAuthProvider: Wraps Auth0 (or other OIDC providers)
 *   - ProtectedRoute: Route guard component
 *   - useVellumAuth: Token management + role resolution hook
 */

// Placeholder — Phase 5 extraction
export {};
